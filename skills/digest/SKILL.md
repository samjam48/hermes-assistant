---
name: digest
description: Morning context compression — where you left off (Phase 2; stub in Phase 1).
version: 0.1.0
metadata:
  hermes:
    tags: [life, context]
    category: productivity
---

# Daily context digest (stub)

**Not fully wired yet.** Target schedule: **08:00 Europe/Lisbon** when built.

## Phase 1

Tell the user:

- `/digest` is coming in Phase 2.
- For now: read yesterday's `digests/*-evening.md` and `inbox/` manually, or run `/loose-ends` for commitments only.
- Breadcrumbs planned: TODO markdown (`memory/paths.md`), pasted browser tab titles (`tabs: ...`), voice/self-notes (`voice: ...` in `note:`).

## Phase 2 outline

1. Read prior evening digest + last 24h inbox.
2. Write `digests/YYYY-MM-DD-morning.md` with: Yesterday's thread · Paused work · Open decisions · Likely blocker · One suggested next action.
3. Optional WhatsApp delivery.

Do not pretend to run full context reconstruction until Phase 2 is implemented.
